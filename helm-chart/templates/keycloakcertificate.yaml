{{- if .Values.keycloak.istio.enabled -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.keycloak.istio.security.credentialName}}
  namespace: istio-system
spec:
  commonName: {{ .Values.keycloak.istio.security.commonName}}
  dnsNames:
    {{- range .Values.keycloak.istio.security.hosts }}
    - {{ . }}
    {{- end }}
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: {{ .Values.keycloak.istio.security.credentialName}}
{{- end }}