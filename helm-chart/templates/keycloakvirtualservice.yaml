{{- if .Values.keycloak.istio.enabled -}}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "keycloak.fullname" . }}-keycloak
spec:
  hosts:
  {{- range .Values.keycloak.istio.hosts }}
    - {{ .host }}
   {{- end }}
  gateways:
    - {{ include "keycloak.fullname" . }}
  http:
    - route:
        - destination:
            host: {{ include "keycloak.fullname" . }}
            port:
              number: {{ .Values.keycloak.service.httpPort }}
{{- end -}}