{{- $fullName := .Release.Name -}}
{{- if .Values.appConfig}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $fullName }}-config
data:
  {{- with .Values.appConfig}}
    {{- toYaml . | nindent 4 }}
  {{- end}}
{{- end}}

